version: '3'

services:
  whatsuppdocker:
    image: fmartinou/whats-up-docker
    container_name: wud
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DATA_ROOT}/whatsupdocker:/store
    ports:
      - ${WUD_PORT}:3000
